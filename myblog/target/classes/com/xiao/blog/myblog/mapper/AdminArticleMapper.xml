<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiao.blog.myblog.mapper.AdminArticleMapper">

  <select id="getAllArticleCount" resultType="int">
   select count(articleId) from article
  </select>
  
  
  <select id="getAllArticleByPage"  parameterType="map"  resultType="com.xiao.blog.myblog.vo.AdminBlogViewModel">
     select a.articleId,a.pubdate,a.articleTitle,b.categoryName,c.username  from article a ,categories b ,user c where a.categoryId = b.categoryId and a.authId = c.uid ORDER BY a.pubdate desc LIMIT #{currentPage}, #{pageSize};
  </select>
  
</mapper>